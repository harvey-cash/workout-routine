export default class Bio {
    name: string
    bodyweight: number
    height: number

    constructor(name: string, bodyweight: number, height: number) {
        this.name = name
        this.bodyweight = bodyweight
        this.height = height
    }
}